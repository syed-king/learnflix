[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python manage.py migrate && python seed.py && gunicorn backend.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
healthcheckPath = "/api/plans/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
